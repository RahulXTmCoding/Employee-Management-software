apply plugin: 'java'

dependencies {
    //local file
    compile files('lib/dl.jar')
    compile files('lib/tmutils.jar')
compile fileTree(dir: 'lib', include: '*.jar')
}
sourceSets {
   main {
      java {
         srcDir 'src'
      }
   }
	
   test {
      java {
         srcDir 'test'
      }
   }
}